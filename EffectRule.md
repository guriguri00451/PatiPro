# 🎰 PatiPro 演出システム詳細仕様書

## 1. `.pati` ファイルの構造 (Asset Package)

`.pati`ファイルは実態をZIP形式とし、リソースと制御ロジックを一つにまとめたパッケージである。

```text
example.pati (ZIP)
├── config.json          # 演出の全定義（ID管理）
└── assets/              # 物理ファイル
    ├── movies/          # mp4
    ├── images/          # png, jpg, svg
    └── sounds/          # mp3, wav

```

## 2. リソース管理ロジック

Webviewのセキュリティ制限により、Reactは直接ファイルを読み込めない。そのため、以下の**ID解決フロー**を必須とする。

1. **IDの定義**: `config.json`内で各ファイルに一意のID（例: `vid_reach_01`）を付与する。
2. **パスの変換**: 拡張機能側が起動時に`asWebviewUri`を用いて、ZIP内の相対パスをWebview用URLに変換する。
3. **マッピングの共有**: 拡張機能からReactへ「ID：URL」の変換テーブルを送信する。
* **Rule**: React側のコンポーネントは、srcに直接パスを書かず、必ずIDからURLを引くこと。



## 3. `config.json` パラメータ定義

演出の「期待度」と「見せ方」を分離して定義する。

### 抽選テーブル (`lottery`)

入賞時に「どの演出を流すか」を決める重み付け設定。

* `probability`: 基本当たり確率（例: 0.003）。
* `patterns`: 演出パターンの配列。
* `id`: パターン識別ID。
* `weight`: 出現比率。
* `is_win`: 当たりフラグ。
* `sequence_id`: 実行する液晶演出のID。



### 演出シーケンス (`sequences`)

液晶で発生するアニメーションのタイムライン。

* `total_duration`: 変動開始から図柄停止までの総時間（ms）。
* `layers`: 同時並行で動くアセットの定義。
* `type`: `video` | `slot` | `effect`
* `resource_id`: 使用するリソースのID。
* `start_time`: 再生開始タイミング（ms）。
* `stop_time`: 終了タイミング。



## 4. 演出実行フロー（液晶制御）

React側は以下のステートマシンに従って描画を切り替える。

1. **待機 (Idle)**: デフォルトのループ動画または静止画を表示。
2. **入賞受信 (Heso-In)**: 拡張機能から抽選結果と演出IDを受信し、保留（ストック）を表示。
3. **変動開始 (Vibration)**:
* 保留を消化し、`sequence_id`に対応する動画・スロットを再生。
* `start_time`に基づき、レイヤーを順次重ねて表示。


4. **リーチ演出 (Reach)**: 特定の時間（ms）経過後、スロットの回転速度やエフェクトを変化させる。
5. **結果表示 (Settlement)**: `is_win`の結果に基づき、図柄を停止。成功時は「BONUS」演出へ移行。

## 5. ハッカソン用実装ガイドライン

* **時間の単位**: 設定ファイル内はすべて **ミリ秒 (ms)** で統一する。
* **アセットの柔軟性**: 動画がない場合は、画像（スプライト）とCSSアニメーションで代用できるよう、`type`による分岐処理をReact側に持たせる。
* **音声の分離**: BGMと効果音（SE）は別レイヤーで管理し、演出の盛り上がりに合わせて重ね掛けできるようにする。

---

この仕様書により、デザイン担当（assets作成）、ロジック担当（config設計）、フロント担当（React実装）の3者が、IDを共通言語としてスムーズに連携できることを目指します。